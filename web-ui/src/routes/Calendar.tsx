import AppShell from '../components/AppShell';

export default function Calendar() {
  return (
    <AppShell title="Calendar">
      <p style={{ color: 'var(--text-dim)', padding: 'var(--sp-4)' }}>Calendar — coming soon</p>
    </AppShell>
  );
}
