import AppShell from '../components/AppShell';

export default function Dashboard() {
  return (
    <AppShell title="Home">
      <p style={{ color: 'var(--text-dim)', padding: 'var(--sp-4)' }}>Dashboard — coming soon</p>
    </AppShell>
  );
}
