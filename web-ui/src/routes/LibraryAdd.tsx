import AppShell from '../components/AppShell';

export default function LibraryAdd() {
  return (
    <AppShell title="Library">
      <p style={{ color: 'var(--text-dim)', padding: 'var(--sp-4)' }}>Library — coming soon</p>
    </AppShell>
  );
}
